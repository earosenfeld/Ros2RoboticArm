# Direct dependencies only. Transitive packages resolve at install time.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.5
numpy>=1.24
aiohttp>=3.9
matplotlib>=3.7      # scripts/make_figures.py
pytest>=7.0

# ROS 2 node scaffolding only (ros2_nodes/camera_node.py); not needed for the web demo:
# opencv-python>=4.9
